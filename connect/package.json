{
  "name": "a2a-connect",
  "version": "0.1.0",
  "description": "MCP server for establishing connections and conversing with A2A agents",
  "license": "MIT",
  "type": "module",
  "bin": {
    "a2a-connect": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "smoke": "npm run build && node tests/smoke.mjs"
  },
  "keywords": [
    "mcp",
    "a2a",
    "agents",
    "model-context-protocol"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
